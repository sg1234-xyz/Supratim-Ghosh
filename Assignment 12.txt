#include<stdio.h>
#include<math.h>
int main()
{
float p,r,n,t;
printf("enter p");// p is principle
scanf("%f",&p);
printf("enter r");// r is rate
scanf("%f",&r);
printf("enter n");// n is per time period
scanf("%f",&n);
printf("enter t");// t is time
scanf("%f",&t);
float a;
a = (1+(r/n));
float b;
b = n*t;
float x;
  x = pow(a,b);
float A;// A is final amount
A = p*x;
printf("the compund interest is %f",&A);
 
 
    return 0;
}
	
